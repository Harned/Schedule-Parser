using System;
using static unixtimehelper.group;
public class Subject{
    string name = "Неизвестно";
    string teacher = "Неизвестен";
    DateTime timeStart = ChangeTime(DateTime.Now);
    DateTime timeStop = ChangeTime(DateTime.Now);
}
